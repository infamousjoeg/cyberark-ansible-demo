- hosts: localhost
  connection: local
  gather_facts: no

  tasks:
    - name: Check Conjur Master Health 
      uri:
        url: https://lxc02.cyberarkdemo.com/health
        return_content: yes
        validate_certs: no
      register: conjur_health
    - name: Report Conjur Master Health
      debug:
        msg: '{{ conjur_health }}'